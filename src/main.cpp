#include <cmath>
#include "mf_3d.h"

int main(int argc, char *argv[]) {
    // auto mf = MagneticField(argv[1]);
    // auto field = mf.get_field(0.25, -0.15, 0.42);
    // std::cout << field[0] << " " << field[1] << " " << field[2] << std::endl;

    auto a = 14.99999999999999999999999999;
    std::cout << std::round(a) << std::endl;

    return EXIT_SUCCESS;
}